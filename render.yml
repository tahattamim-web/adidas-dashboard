services:
  - type: web
    name: adidas-dashboard
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn adidas_dashboard:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
